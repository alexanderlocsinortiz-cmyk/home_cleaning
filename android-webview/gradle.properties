org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true

# Debug emulator testing against `php artisan serve` on the host machine only.
# Release tasks reject this HTTP value. Pass the real HTTPS host explicitly:
# ./gradlew bundleRelease -PcleanflowUrl=https://your-real-host.example
cleanflowUrl=http://10.0.2.2:8000
